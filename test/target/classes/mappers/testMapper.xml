<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.mappers.test">

	<select id="list" resultType="com.test.domain.testVO">
		select 
			data3, productid, data2, data4, data1, timestamp, verify
		from offer	
	</select>
	
	<!-- 상품 등록 -->
	<insert id="register">
		insert into offer (data3, data2, productid, data4, data1,verify)
		values (#{data3}, #{data2}, #{productid}, #{data4}, #{data1},0)
	</insert>
	
	<!-- 고객요청 리스트 -->
	<select id="offerlist" resultType="com.test.domain.testVO">
		select 
			idx, data3, productid, data2, data4, data1, timestamp, verify
		from offer	
	</select>
	
	<!-- 고객요청 상품보기 -->	
	<select id="view" resultType="com.test.domain.testVO">
		select 
			idx, data3, productid, data2, data4, data1 
		from offer	
		where idx = #{idx}
	</select>
	
</mapper>