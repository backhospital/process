<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.mappers.list">
	<select id="list" resultType="com.mall.vo.listVO">
		select 
			data3, productid, data2, data4, data1, timestamp, verify
		from display	
	</select>
</mapper>